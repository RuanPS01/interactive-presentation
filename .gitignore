# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

node_modules
dist
dist-ssr
*.local

# Env (contém as chaves do Firebase para desenvolvimento local)
.env
.env.local
.env.*.local

# Firebase CLI: cache local e a conta de serviço usada em deploys manuais.
# O `.firebaserc` não é versionado de propósito — o id do projeto vai pela
# secret VITE_FIREBASE_PROJECT_ID (ver .github/workflows/deploy.yml).
.firebase/
.firebaserc
firebase-debug.log
*service-account*.json

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
